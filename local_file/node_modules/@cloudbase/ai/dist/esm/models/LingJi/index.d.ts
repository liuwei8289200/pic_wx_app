import { type ParsedEvent } from 'eventsource-parser/stream';
import type { LingJiInputData, LingJiGenerateTextOutput, LingJiStreamTextOutput } from './type';
import type { ChatModel, StreamTextResult, ModelReq } from '../../type';
export declare class LingJiModel implements ChatModel {
    private req;
    baseUrl: string;
    subUrl: string;
    constructor(req: ModelReq, baseUrl: string, subUrl?: string);
    private get url();
    generateText(data: LingJiInputData): Promise<{
        text: string;
        rawResponse: LingJiGenerateTextOutput;
    }>;
    streamText(data: LingJiInputData): Promise<LingJiModelStreamResult>;
}
declare class LingJiModelStreamResult implements StreamTextResult {
    private _eventSourceStream;
    constructor(_stream: ReadableStream<Uint8Array>);
    private get teeedStream();
    get eventSourceStream(): import("../../type").AsyncIterableReadableStream<ParsedEvent>;
    get dataStream(): import("../../type").AsyncIterableReadableStream<LingJiStreamTextOutput>;
    get textStream(): import("../../type").AsyncIterableReadableStream<string>;
}
export {};
